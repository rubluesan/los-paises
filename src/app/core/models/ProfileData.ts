export interface ProfileData {
  id: string;
  username: string;
  avatar_url: string;
  updated_at: string;
}

export interface Username {
  username: string;
}

export interface AvatarUrl {
  avatar_url: string;
}
